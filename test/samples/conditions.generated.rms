<LAND_GENERATION>
  if DESERT_MAP
    base_terrain DIRT
  else
    base_terrain GRASS3
  endif
  create_player_lands {
    if DESERT_MAP
      terrain_type DIRT
    else
      terrain_type GRASS3
    endif
    land_percent 25
  }

if FOO
  <PLAYER_SETUP>
    random_placement
else
  <PLAYER_SETUP>
    random_placement
endif

if FOO
  if FOO
    <PLAYER_SETUP>
      random_placement
  else
    <PLAYER_SETUP>
      random_placement
  endif
elseif BAR
  if FOO
    <PLAYER_SETUP>
      random_placement
  else
    <PLAYER_SETUP>
      random_placement
  endif
else
  if FOO
    <PLAYER_SETUP>
      random_placement
  else
    <PLAYER_SETUP>
      random_placement
  endif
endif
